---
title: React Quick Start
description: Get AUTOUI running in your React app in under 5 minutes. Step-by-step guide for React applications.
keywords: ["react", "quickstart", "getting started", "create react app", "vite"]
---

This guide will help you integrate AUTOUI into your React application quickly.

## Prerequisites

- A React application (Create React App, Vite, or any React setup)
- Node.js 16 or higher
- An AUTOUI app registration (see step 2 below)

## Step 1: Install AUTOUI

```bash
npm i @autoai-ui/autoui
```

## Step 2: Register your app (hosted portal)

Go to `https://autoui-chi.vercel.app/` and:

- Login / Sign up
- Create a new app
- Paste your **OpenRouter API key**
- Copy your **`appId`** and **`sharedSecret`**

## Step 3: Configure Vite Plugin (Vite projects only)

If you're using Vite, add the AutoUI type schema plugin to your `vite.config.ts`:

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { autouiTypeSchemaPlugin } from '@autoai-ui/autoui/plugin';

export default defineConfig({
  plugins: [
    react(),
    autouiTypeSchemaPlugin(),
  ],
});
```

This plugin generates a `.autoui-runtime-schema.json` file during development builds. Commit this file to your repo so production builds can validate too.

Skip this step if you're using Create React App.

## Step 4: Create Your Config

Create a new file `lib/autoui-config.ts`:

**For Vite projects:**
```tsx
import type { AutoUIConfig } from '@autoai-ui/autoui';

export const autouiConfig: AutoUIConfig = {
  appId: import.meta.env.VITE_AUTOUI_APP_ID || 'my-react-app',
  llm: {
    proxyUrl: import.meta.env.VITE_AUTOUI_PROXY_URL || 'https://autoui-proxy.onrender.com',
    sharedSecret: import.meta.env.VITE_AUTOUI_SHARED_SECRET,
  },
  runtime: {
    validateLLMOutput: true,
    storeChatToLocalStorage: true,
    maxSteps: 10,
  },
  functions: {},
  components: {},
};
```

**For Create React App:**
```tsx
import { AutoUIConfig } from "@autoai-ui/autoui"

export function createAutoUIConfig(): AutoUIConfig {
  return {
    appId: process.env.REACT_APP_AUTOUI_APP_ID || 'my-react-app',
    llm: {
      proxyUrl: process.env.REACT_APP_AUTOUI_PROXY_URL || 'https://autoui-proxy.onrender.com',
      sharedSecret: process.env.REACT_APP_AUTOUI_SHARED_SECRET,
    },
    runtime: {
      validateLLMOutput: true,
      storeChatToLocalStorage: true,
      maxSteps: 10,
    },
    functions: {},
    components: {},
  }
}
```

## Step 5: Add Environment Variables

Create or update your `.env` file:

**For Vite projects:**
```bash
#
# IMPORTANT: must match VITE_AUTOUI_APP_ID (plugin reads AUTOUI_APP_ID)
AUTOUI_APP_ID=your-app-id
VITE_AUTOUI_APP_ID=your-app-id
VITE_AUTOUI_PROXY_URL=https://autoui-proxy.onrender.com
VITE_AUTOUI_SHARED_SECRET=your-shared-secret-here
```

**For Create React App:**
```bash
REACT_APP_AUTOUI_APP_ID=your-app-id
REACT_APP_AUTOUI_PROXY_URL=https://autoui-proxy.onrender.com
REACT_APP_AUTOUI_SHARED_SECRET=your-shared-secret-here
```

<Note title="Environment Variables" type="info">
For Create React App, environment variables must start with `REACT_APP_`. For Vite, use `VITE_` prefix instead. Also note that you'll need to register your app with AutoUI service to get your `appId` and `shared_secret`. See the [Complete Developer Guide](/docs/developer-guide) for details.
</Note>

## Step 6: Add ModalChat to Your App

Import and render the ModalChat component in your main app file:

```tsx
// App.tsx or your main component
import { ModalChat } from "@autoai-ui/autoui"
import { createAutoUIConfig } from "./lib/autoui-config"

function App() {
  const config = createAutoUIConfig()

  return (
    <div className="App">
      <h1>My React App</h1>
      {/* Your app content */}
      
      <ModalChat config={config} />
    </div>
  )
}

export default App
```

## Step 7: Start Your App

```bash
# Vite
npm run dev

# Create React App
# npm start
```

You should now see a floating chat button in the bottom-right corner of your app!

## Next Steps

- Read the [Complete Developer Guide](/docs/developer-guide) for comprehensive setup instructions
- Learn about [configuration options](/docs/config)
- Add [functions and components](/docs/functions) to extend capabilities
- Check out [React best practices](/docs/best-practices/react)

## Troubleshooting

If you encounter issues:

- Make sure your backend proxy server is running
- Verify your environment variables are set correctly
- Check the browser console for error messages
- See the [Troubleshooting guide](/docs/troubleshooting)
